#include <iostream>

using namespace std;

int main () {
    int x = 10;
    int y = 9;
    cout << (x > y) << endl; // returns 1 (true), because 10 is higher than 9
    int a = 10;
    cout << (a == 10) << endl;  // returns 1 (true), because the value of x is equal to 10
    return 0;
}